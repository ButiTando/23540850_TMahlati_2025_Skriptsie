"""Device simulators for the Ithemba control station.

    python -m simulators.degrader
    python -m simulators.dosimeter

Both default to imitating the real firmware, quirks included, so that what
works here works on the bench. Flags relax that and inject faults.
"""
